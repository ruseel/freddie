[package]
name = "freddie_sync"
description = "The two-phase sync of a mirrored value: the Synced entry and the correlation token its reads ride."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
# Test-only equality on RidingGeneration, so a type that carries one can derive PartialEq
# under testing. Production has no PartialEq: the token is not comparable.
testing = []

[lints]
workspace = true
